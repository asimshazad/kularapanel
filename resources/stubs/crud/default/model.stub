<?php

namespace {model_namespace};

use Illuminate\Database\Eloquent\Model as Eloquent;

{use_class_media_library}
    {use_model_traits}

class {model_class} extends Eloquent {model_implements}
{
    {use_model_traits_name}

    protected $dates = [{dates_attributes}];
    protected $fillable = [{model_fillable_attribute}];
    {timestamps_model}
    {model_appends}

    {model_casts}

    {model_const}

    {relationships}

    {user_timezones}

    {mutators}

    {media_conversion}
}
