                        <crop-image
                            :crop-width="{{{crop_width}}}"
                            :ratio="{{{crop_ratio}}}"
                            input-name="crop_image"
                            :return-base64="true"
                        ></crop-image>
