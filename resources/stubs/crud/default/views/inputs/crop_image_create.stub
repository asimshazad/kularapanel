                        <crop-image
                            :crop-width="{{{crop_width}}}"
                            :ratio="{{{crop_ratio}}}"
                            input-name="{input_name}"
                            :return-base64="true"
                        ></crop-image>
