<drop-zone
    _token="{{ csrf_token() }}"
    url="{{route('admin.{model_variables}.upload_images', ['collect'=>{dropzone_collection_name}])}}"
    :add-to-editor="{add_dropzone_to_editor}"
></drop-zone>
